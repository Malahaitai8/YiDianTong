# 易点通挂号系统 - 完整部署教程

> 从零开始，完整部署易点通挂号系统到华为云服务器的详细教程

## 📋 目录

- [系统架构](#系统架构)
- [前置准备](#前置准备)
- [第一步：安装Docker和Docker Compose](#第一步安装docker和docker-compose)
- [第二步：准备服务器环境](#第二步准备服务器环境)
- [第三步：部署应用](#第三步部署应用)
- [第四步：配置小程序](#第四步配置小程序)
- [运维管理](#运维管理)
- [故障排查](#故障排查)
- [快速参考](#快速参考)

---

## 🏗️ 系统架构

```
┌─────────────────┐
│   小程序客户端    │  (体验版，无需容器化)
│  (微信小程序)     │
└────────┬────────┘
         │ HTTPS/WSS
         │
┌────────▼─────────────────────────────────┐
│           华为云服务器                      │
│  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ Frontend │  │ Backend  │  │  Redis  │ │
│  │  (Nginx) │  │(SpringBoot)│ │         │ │
│  │   :80    │  │  :8080   │  │  :6379  │ │
│  └──────────┘  └──────────┘  └─────────┘ │
└──────────────────────────────────────────┘
         │
         │ JDBC
         │
┌────────▼────────┐
│   远程MySQL      │
│ 120.46.206.75   │
└─────────────────┘
```

---

## ✅ 前置准备

### 1. 服务器要求

- **操作系统**: Linux (推荐 Ubuntu 20.04+ 或 CentOS 7+)
- **CPU**: 2核以上
- **内存**: 4GB以上
- **磁盘**: 20GB以上可用空间
- **网络**: 公网IP

### 2. 华为云安全组配置

在华为云控制台配置安全组规则：

| 方向 | 协议 | 端口 | 说明 |
|------|------|------|------|
| 入方向 | TCP | 80 | HTTP访问 |
| 入方向 | TCP | 443 | HTTPS访问（如使用SSL） |
| 入方向 | TCP | 8080 | 后端API（可选，建议仅内网） |
| 入方向 | TCP | 22 | SSH远程连接 |
| 出方向 | 全部 | - | 允许所有出站流量 |

### 3. 域名配置（可选但推荐）

- 将域名解析到服务器公网IP
- 配置SSL证书（Let's Encrypt免费证书）

---

## 第一步：安装Docker和Docker Compose

⚠️ **这是必须步骤！** 如果未安装Docker和Docker Compose，无法进行后续部署。

### 方法一：自动安装（推荐）

```bash
# 1. 连接到华为云服务器
ssh root@你的服务器IP

# 2. 更新系统包
apt update  # Ubuntu/Debian
# 或
yum update -y  # CentOS/RHEL

# 3. 使用官方脚本自动安装Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 4. 安装Docker Compose插件
apt install docker-compose-plugin -y  # Ubuntu/Debian
# 或
yum install docker-compose-plugin -y  # CentOS/RHEL

# 5. 启动Docker服务
systemctl start docker
systemctl enable docker

# 6. 验证安装
docker --version
docker compose version
docker run hello-world  # 应该看到 "Hello from Docker!"
```



### 配置Docker镜像加速（国内服务器推荐）

```bash
# 创建或编辑配置文件
vi /etc/docker/daemon.json
```

添加以下内容：

```json
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com",
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
```

重启Docker服务：

```bash
systemctl daemon-reload
systemctl restart docker
```

### 安装验证检查清单

- [ ] Docker已安装：`docker --version` 显示版本号
- [ ] Docker Compose已安装：`docker compose version` 显示版本号
- [ ] Docker服务正在运行：`systemctl status docker` 显示 active
- [ ] 可以运行测试容器：`docker run hello-world` 成功

---

## 第二步：准备服务器环境

### 1. 上传项目代码

```bash
# 创建项目目录
mkdir -p /opt/yidiantong
cd /opt/yidiantong

# 方式1: 使用Git克隆（如果有仓库）
git clone <你的仓库地址> .

# 方式2: 使用scp从本地上传
# 在本地执行: scp -r . root@服务器IP:/opt/yidiantong/
```

### 2. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
vi .env
```

**必须配置的项**：

```env
# 数据库配置
DB_HOST=120.46.206.75
DB_PORT=3306
DB_NAME=yi_dian_tong
DB_USERNAME=root
DB_PASSWORD=你的数据库密码  # ⚠️ 必须修改

# JWT密钥（生产环境必须修改为强密码）
JWT_SECRET=你的强密码密钥  # ⚠️ 必须修改，建议32位以上随机字符串

# 微信小程序配置（必须配置）
WECHAT_APPID=你的小程序AppID  # ⚠️ 必须配置
WECHAT_SECRET=你的小程序Secret  # ⚠️ 必须配置

# DeepSeek API密钥
DEEPSEEK_API_KEY=你的API密钥

# Redis密码（可选，建议配置）
REDIS_PASSWORD=
```

### 3. 创建日志目录

```bash
mkdir -p springboot/logs
```

---

## 第三步：部署应用

### 1. 构建Docker镜像

```bash
# 进入项目根目录
cd /opt/yidiantong

# 构建镜像（首次部署，可能需要10-20分钟）
docker compose -f docker-compose.prod.yml build
```

### 2. 启动所有服务

```bash
# 启动所有服务
docker compose -f docker-compose.prod.yml up -d

# 查看服务状态
docker compose -f docker-compose.prod.yml ps
```

应该看到三个服务都在运行：
- `ydt-redis` - Redis服务
- `ydt-backend` - SpringBoot后端
- `ydt-frontend` - 前端管理界面

### 3. 验证部署

```bash
# 检查后端健康状态
curl http://localhost:8080/hello
# 应该返回: hello

# 检查前端
curl http://localhost
# 应该返回HTML内容

# 查看所有容器状态
docker ps
```

### 4. 查看日志

```bash
# 查看所有服务日志
docker compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker compose -f docker-compose.prod.yml logs -f backend
docker compose -f docker-compose.prod.yml logs -f frontend
docker compose -f docker-compose.prod.yml logs -f redis
```

### 5. 配置Nginx反向代理（可选，使用域名时推荐）

如果需要使用域名和SSL：

```bash
# 安装Nginx
apt install nginx -y  # Ubuntu
# 或
yum install nginx -y  # CentOS

# 创建配置文件
vi /etc/nginx/sites-available/yidiantong
```

Nginx配置示例：

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    # 前端
    location / {
        proxy_pass http://localhost:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 后端API
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket支持
    location /ws {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

启用配置并重启Nginx：

```bash
ln -s /etc/nginx/sites-available/yidiantong /etc/nginx/sites-enabled/
nginx -t  # 测试配置
systemctl restart nginx
```

---

## 第四步：配置小程序

### 1. 在微信公众平台配置服务器域名

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入 **开发** -> **开发管理** -> **开发设置**
3. 配置 **服务器域名**:
   - **request合法域名**: `https://你的域名.com` 或 `https://你的服务器IP:8080`
   - **socket合法域名**: `wss://你的域名.com` 或 `wss://你的服务器IP:8080`
   - **uploadFile合法域名**: `https://你的域名.com`
   - **downloadFile合法域名**: `https://你的域名.com`

> ⚠️ **注意**: 小程序要求使用HTTPS/WSS，如果使用IP地址，需要配置SSL证书

### 2. 修改小程序API配置

编辑 `client-mini-app/src/config/index.js`:

```javascript
// 从环境变量读取API基础地址
const baseURL = process.env.VUE_APP_API_BASE_URL || 'https://你的域名.com/api'
// 或使用IP（需要HTTPS）: 'https://你的服务器IP:8080'

// 开发环境与生产环境配置
const config = {
  development: {
    baseURL: baseURL,
    timeout: 10000
  },
  production: {
    baseURL: baseURL,
    timeout: 10000
  }
}
```

### 3. 构建小程序体验版

在本地开发环境执行：

```bash
cd client-mini-app
npm install
npm run build:mp-weixin
```

构建完成后，使用微信开发者工具打开 `client-mini-app/dist/build/mp-weixin` 目录，上传体验版。

### 4. 配置环境变量（可选）

在 `client-mini-app` 目录创建 `.env.production`:

```env
VUE_APP_API_BASE_URL=https://你的域名.com/api
```

---

## 🔧 运维管理

### 常用命令

```bash
# 查看服务状态
docker compose -f docker-compose.prod.yml ps

# 查看日志
docker compose -f docker-compose.prod.yml logs -f
docker compose -f docker-compose.prod.yml logs -f backend
docker compose -f docker-compose.prod.yml logs -f frontend
docker compose -f docker-compose.prod.yml logs -f redis

# 重启服务
docker compose -f docker-compose.prod.yml restart
docker compose -f docker-compose.prod.yml restart backend
docker compose -f docker-compose.prod.yml restart frontend

# 停止服务
docker compose -f docker-compose.prod.yml stop

# 启动服务
docker compose -f docker-compose.prod.yml start

# 停止并删除容器
docker compose -f docker-compose.prod.yml down

# 停止并删除容器和卷
docker compose -f docker-compose.prod.yml down -v

# 重新构建并启动
docker compose -f docker-compose.prod.yml up -d --build
```

### 更新部署

```bash
# 1. 进入项目目录
cd /opt/yidiantong

# 2. 拉取最新代码
git pull
# 或重新上传代码

# 3. 重新构建镜像
docker compose -f docker-compose.prod.yml build

# 4. 重启服务（零停机）
docker compose -f docker-compose.prod.yml up -d
```

### 数据备份

```bash
# Redis数据备份
docker exec ydt-redis redis-cli BGSAVE

# 查看日志
tail -f /opt/yidiantong/springboot/logs/application.log

# 导出Redis数据
docker exec ydt-redis redis-cli --rdb /data/dump.rdb
```

### 监控和资源使用

```bash
# 查看容器资源使用情况
docker stats

# 查看磁盘使用
docker system df

# 清理未使用的镜像和容器
docker system prune -a
```

---

## 🐛 故障排查

### 问题1: Docker安装失败

**症状**: `docker --version` 无输出或报错

**解决方案**:
```bash
# 检查Docker服务状态
systemctl status docker

# 如果未运行，启动服务
systemctl start docker
systemctl enable docker

# 查看错误日志
journalctl -u docker
```

### 问题2: 后端无法启动

**症状**: `ydt-backend` 容器状态为 `Exited`

**解决方案**:
```bash
# 查看详细日志
docker logs ydt-backend

# 常见原因和解决方法:
# 1. 数据库连接失败
#    - 检查 .env 文件中的 DB_HOST, DB_PASSWORD
#    - 测试数据库连接: docker exec ydt-backend ping 120.46.206.75

# 2. Redis连接失败
#    - 检查Redis容器是否运行: docker ps | grep redis
#    - 检查Redis连接: docker exec ydt-redis redis-cli ping

# 3. 端口被占用
#    - 检查端口: netstat -tulpn | grep 8080
#    - 修改 docker-compose.prod.yml 中的端口映射
```

### 问题3: 前端无法访问后端

**症状**: 前端页面显示网络错误或502

**解决方案**:
```bash
# 检查后端健康状态
curl http://localhost:8080/hello

# 检查Docker网络
docker network inspect ydt-network

# 检查Nginx配置
docker exec ydt-frontend cat /etc/nginx/conf.d/default.conf

# 重启前端容器
docker compose -f docker-compose.prod.yml restart frontend
```

### 问题4: 小程序无法连接服务器

**症状**: 小程序请求失败，提示网络错误

**解决方案**:
1. **检查服务器域名配置**
   - 确认已在微信公众平台配置服务器域名
   - 域名必须使用HTTPS（不能是HTTP）

2. **检查防火墙和安全组**
   ```bash
   # 检查防火墙
   systemctl status firewalld
   # 开放端口
   firewall-cmd --permanent --add-port=80/tcp
   firewall-cmd --permanent --add-port=8080/tcp
   firewall-cmd --reload
   ```

3. **检查SSL证书**
   - 如果使用域名，确保SSL证书有效
   - 测试: `curl https://你的域名.com/api/hello`

4. **查看后端日志**
   ```bash
   docker logs ydt-backend | grep -i error
   ```

### 问题5: Redis连接失败

**症状**: 后端日志显示Redis连接错误

**解决方案**:
```bash
# 检查Redis状态
docker exec ydt-redis redis-cli ping
# 应该返回: PONG

# 如果配置了密码
docker exec ydt-redis redis-cli -a 你的密码 ping

# 重启Redis
docker compose -f docker-compose.prod.yml restart redis
```

### 问题6: 数据库连接失败

**症状**: 后端日志显示数据库连接超时

**解决方案**:
```bash
# 测试网络连接
docker exec ydt-backend ping 120.46.206.75

# 检查数据库配置
docker exec ydt-backend env | grep DB_

# 确认数据库服务器允许远程连接
# 检查MySQL的bind-address配置
```

### 问题7: 容器资源不足

**症状**: 容器频繁重启或运行缓慢

**解决方案**:
```bash
# 查看资源使用
docker stats

# 清理未使用的资源
docker system prune -a

# 如果内存不足，考虑升级服务器配置
```

---

## 📝 快速参考

### 一键部署脚本

如果项目中有 `deploy.sh` 脚本：

```bash
chmod +x deploy.sh
./deploy.sh
```

### 最简部署流程

```bash
# 1. 安装Docker
curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh
apt install docker-compose-plugin -y
systemctl start docker && systemctl enable docker

# 2. 配置环境变量
cp .env.example .env
vi .env  # 修改必要配置

# 3. 构建并启动
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d

# 4. 验证
curl http://localhost:8080/hello
curl http://localhost
```

### 重要配置检查清单

部署前确认：
- [ ] Docker和Docker Compose已安装并运行
- [ ] `.env` 文件已配置，包含数据库密码
- [ ] `.env` 文件中的 `JWT_SECRET` 已修改为强密码
- [ ] 微信小程序 `WECHAT_APPID` 和 `WECHAT_SECRET` 已配置
- [ ] 华为云安全组已开放必要端口（80, 8080, 22）
- [ ] 服务器有足够的资源（CPU、内存、磁盘）

部署后验证：
- [ ] 后端服务正常：`curl http://localhost:8080/hello`
- [ ] 前端服务正常：`curl http://localhost`
- [ ] Redis服务正常：`docker exec ydt-redis redis-cli ping`
- [ ] 所有容器运行中：`docker ps`
- [ ] 小程序可以正常访问API

---

## ⚠️ 重要注意事项

### 安全性

1. **生产环境必须修改**:
   - `JWT_SECRET`: 使用32位以上随机字符串
   - 数据库密码: 不要使用默认密码
   - Redis密码: 建议配置密码

2. **使用HTTPS**:
   - 小程序要求使用HTTPS/WSS
   - 配置SSL证书（Let's Encrypt免费证书）

3. **防火墙配置**:
   - 只开放必要的端口
   - 8080端口建议仅内网访问

### 性能优化

1. **数据库连接池**: 根据实际负载调整 `application-prod.yml` 中的连接池大小
2. **Redis持久化**: 已配置AOF持久化，数据会自动保存
3. **日志管理**: 定期清理日志文件，避免磁盘空间不足

### 备份策略

1. **数据库备份**: 定期备份MySQL数据库
2. **Redis备份**: 定期导出Redis数据
3. **配置文件备份**: 保存 `.env` 文件副本

### 小程序限制

1. **域名白名单**: 小程序只能访问配置的合法域名
2. **HTTPS要求**: WebSocket必须使用 `wss://`（HTTPS）
3. **体验版测试**: 确保体验版可以正常访问所有功能

---

## 📞 获取帮助

如遇到问题，请按以下顺序检查：

1. **查看日志**: `docker compose -f docker-compose.prod.yml logs -f`
2. **检查服务状态**: `docker compose -f docker-compose.prod.yml ps`
3. **验证网络连接**: 测试数据库、Redis连接
4. **检查配置文件**: 确认 `.env` 文件配置正确
5. **查看资源使用**: `docker stats` 检查资源是否充足

---

**部署完成后，小程序体验版即可正常使用！** 🎉

如有问题，请参考故障排查章节或查看详细的日志信息。

