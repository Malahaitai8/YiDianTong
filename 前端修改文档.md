# 前端修改文档 - Bug修复协同

## 文档说明
本文档列出了后端修复后，前端需要同步修改的内容，以确保前后端协同完成所有bug的修复。

---

## 🔴 高优先级修改

### 1. 手机号唯一性校验错误提示

**涉及文件：**
- `client-mini-app/src/pages/register/register.vue` - 注册页面
- `client-mini-app/src/pages/edit-info/edit-info.vue` - 修改信息页面
- `frontend/src/views/admin/Patients.vue` - 后台患者管理

**修改内容：**
后端已添加手机号唯一性校验，当手机号重复时会返回错误信息。前端需要：

1. **注册页面** (`register.vue`)：
   - 已存在手机号格式校验，无需修改
   - 确保错误提示能正确显示后端返回的"该手机号已被注册"错误信息

2. **修改信息页面** (`edit-info.vue`)：
   - 已存在手机号格式校验，无需修改
   - 确保错误提示能正确显示后端返回的"该手机号已被其他用户使用"错误信息

3. **后台患者管理** (`Patients.vue`)：
   - 确保添加/编辑患者时，能正确显示后端返回的手机号重复错误信息

**后端返回的错误信息：**
- 注册时：`"该手机号已被注册，请使用其他手机号"`
- 修改信息时：`"该手机号已被其他用户使用，请使用其他手机号"`

---

### 2. 医生启用功能

**涉及文件：**
- `frontend/src/views/admin/Doctors.vue` - 医生管理页面
- `frontend/src/api/doctor.js` - 医生API

**修改内容：**

1. **添加启用接口** (`frontend/src/api/doctor.js`)：
```javascript
// 启用医生账号
export const enableDoctor = (id) => {
  return request({
    url: `/admin/doctor/${id}/enable`,
    method: 'post'
  })
}
```

2. **修改医生列表操作按钮** (`Doctors.vue`)：
```vue
<!-- 在操作列中，根据状态显示不同按钮 -->
<el-button
  type="danger"
  size="small"
  @click="handleDisableDoctor(row)"
  v-if="row.user?.status === 'active'"
>
  <el-icon><CircleClose /></el-icon>
  停用
</el-button>

<!-- 新增：停用状态显示启用按钮 -->
<el-button
  type="success"
  size="small"
  @click="handleEnableDoctor(row)"
  v-if="row.user?.status === 'disabled'"
>
  <el-icon><CircleCheck /></el-icon>
  启用
</el-button>
```

3. **添加启用方法** (`Doctors.vue`)：
```javascript
// 启用医生
const handleEnableDoctor = async (doctor) => {
  try {
    await ElMessageBox.confirm(
      `确定要启用医生 ${doctor.name} 吗？`,
      '确认操作',
      { type: 'warning' }
    )
    
    await enableDoctor(doctor.user.id)
    ElMessage.success('医生账号已启用')
    await loadDoctors() // 重新加载数据
  } catch (error) {
    if (error !== 'cancel') {
      console.error('启用失败:', error)
      ElMessage.error('启用失败: ' + (error.message || '未知错误'))
    }
  }
}
```

---

## 🟡 中优先级修改

### 3. 患者认证状态审核

**涉及文件：**
- `frontend/src/views/admin/Patients.vue` - 患者管理页面
- `frontend/src/api/patient.js` - 患者API

**修改内容：**

1. **添加审核接口** (`frontend/src/api/patient.js`)：
```javascript
// 审核患者认证状态
export const approvePatient = (id) => {
  return request({
    url: `/patient/${id}/approve`,
    method: 'post'
  })
}
```

2. **添加审核按钮和方法** (`Patients.vue`)：
```vue
<!-- 在操作列中添加审核按钮（仅对pending状态显示） -->
<el-button
  type="success"
  size="small"
  @click="handleApprovePatient(row)"
  v-if="row.idStatus === 'pending' || row.idStatus === '待认证'"
>
  <el-icon><Check /></el-icon>
  审核通过
</el-button>
```

```javascript
// 审核患者
const handleApprovePatient = async (patient) => {
  try {
    await ElMessageBox.confirm(
      `确定要审核通过患者 ${patient.name} 的身份认证吗？`,
      '确认操作',
      { type: 'warning' }
    )
    
    await approvePatient(patient.id)
    ElMessage.success('审核成功')
    await loadPatients() // 重新加载数据，确保状态更新
  } catch (error) {
    if (error !== 'cancel') {
      console.error('审核失败:', error)
      ElMessage.error('审核失败: ' + (error.message || '未知错误'))
    }
  }
}
```

---

### 4. 用户名和手机号格式校验

**涉及文件：**
- `client-mini-app/src/pages/register/register.vue` - 注册页面

**修改内容：**

后端已添加用户名长度和格式校验（4-20字符，只允许字母、数字、下划线）。前端需要同步：

```javascript
// 在handleRegister方法中添加用户名校验
if (!username || username.length < 4 || username.length > 20) {
  uni.showToast({
    title: '用户名长度必须在4-20个字符之间',
    icon: 'none'
  });
  return;
}

if (!/^[a-zA-Z0-9_]+$/.test(username)) {
  uni.showToast({
    title: '用户名只能包含字母、数字和下划线',
    icon: 'none'
  });
  return;
}
```

---

## 🟢 低优先级修改

### 5. 科室描述字数限制

**涉及文件：**
- `frontend/src/views/admin/Departments.vue` - 科室管理页面

**修改内容：**

后端已添加科室描述200字符限制。前端需要同步：

```vue
<el-form-item label="科室描述" prop="description">
  <el-input
    v-model="departmentForm.description"
    type="textarea"
    :rows="4"
    placeholder="请输入科室描述"
    maxlength="200"
    show-word-limit
  />
</el-form-item>
```

在表单验证规则中添加：
```javascript
const departmentRules = {
  name: [
    { required: true, message: '请输入科室名称', trigger: 'blur' },
    { min: 2, max: 50, message: '科室名称长度在 2 到 50 个字符', trigger: 'blur' }
  ],
  description: [
    { max: 200, message: '科室描述长度不能超过200个字符', trigger: 'blur' }
  ]
}
```

---

### 6. 真实姓名格式校验

**涉及文件：**
- `client-mini-app/src/pages/personal-info/personal-info.vue` - 身份认证页面

**修改内容：**

后端已添加真实姓名格式校验（只允许汉字或英文字母）。前端需要同步：

```javascript
// 在handleVerify方法中添加真实姓名格式校验
if (!/^[\u4e00-\u9fa5a-zA-Z]+$/.test(name)) {
  uni.showToast({
    title: '真实姓名只能包含汉字或英文字母',
    icon: 'none'
  });
  return;
}
```

---

### 7. 医生姓名长度校验

**涉及文件：**
- `frontend/src/views/admin/Doctors.vue` - 医生管理页面

**修改内容：**

后端已添加医生姓名长度校验（2-15字符）。前端需要同步：

```javascript
const doctorRules = {
  // ... 其他规则
  name: [
    { required: true, message: '请输入姓名', trigger: 'blur' },
    { min: 2, max: 15, message: '医生姓名长度必须在2-15个字符之间', trigger: 'blur' }
  ],
  // ...
}
```

---

### 8. 按科室查询显示门诊信息

**涉及文件：**
- `client-mini-app/src/pages/department-list/department-list.vue` - 科室列表页面
- `client-mini-app/src/api/clinic.js` - 门诊API

**修改内容：**

后端已添加根据科室ID查询门诊的接口。前端可以优化：

1. **更新API调用** (`client-mini-app/src/api/clinic.js`)：
```javascript
/**
 * 根据科室ID获取门诊列表（使用后端接口）
 * @param {number|string} departmentId 科室ID
 * @returns {Promise<Array>} 门诊列表
 */
export function getClinicsByDepartmentId(departmentId) {
    return request({
        url: `/clinic/selectByDepartmentId/${departmentId}`,
        method: 'GET'
    })
}
```

2. **科室列表页面显示门诊信息** (`department-list.vue`)：
如果需要在小程序首页的科室列表中直接显示门诊信息，可以：

```vue
<!-- 在科室卡片中显示门诊列表 -->
<view class="department-card" v-for="department in departments" :key="department.id">
  <view class="dept-name">{{ department.name }}</view>
  <!-- 显示该科室下的门诊 -->
  <view class="clinic-tags" v-if="department.clinics && department.clinics.length > 0">
    <text 
      class="clinic-tag" 
      v-for="clinic in department.clinics" 
      :key="clinic.id"
    >
      {{ clinic.name }}
    </text>
  </view>
</view>
```

---

## 📋 修改检查清单

- [ ] 注册页面：用户名格式校验（4-20字符，字母数字下划线）
- [ ] 注册页面：手机号唯一性错误提示
- [ ] 修改信息页面：手机号唯一性错误提示
- [ ] 后台患者管理：手机号唯一性错误提示
- [ ] 后台患者管理：添加审核按钮和方法
- [ ] 医生管理：添加启用按钮和方法
- [ ] 医生管理：医生姓名长度校验（2-15字符）
- [ ] 科室管理：科室描述字数限制（200字符）
- [ ] 身份认证：真实姓名格式校验（汉字或字母）
- [ ] 科室列表：使用后端接口查询门诊（可选优化）

---

## ⚠️ 注意事项

1. **错误处理**：确保所有API调用都有错误处理，能正确显示后端返回的错误信息
2. **数据刷新**：操作成功后（如审核、启用等），记得重新加载列表数据
3. **用户体验**：添加loading状态，操作前确认提示等
4. **兼容性**：确保修改不影响现有功能
5. **数据库**：需要执行`BUG_FIX_SQL.sql`添加手机号唯一索引（防止并发重复插入）

---

## 📞 联系方式

如有疑问，请联系后端开发团队。

