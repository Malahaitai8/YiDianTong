# **医点通医院预约挂号系统 \- 缺陷修复方案汇总**

文档版本：V1.0  
文档说明：本文档基于《系统测试报告》编写，旨在为开发团队提供详尽的 Bug 描述与具体的修改方案。

## **🔴 高优先级 (必须立即修复)**

此类缺陷严重影响系统的核心业务流程（如注册、认证）或数据一致性，必须在下一版本发布前完成修复。

### **1\. 手机号唯一性校验失效**

* **Bug 编号**: BUG-PATM-001, BUG-PAT-01, BUG-PAT-02  
* **涉及模块**: 患者管理（后台）、用户注册（小程序）、个人信息修改（小程序）  
* **问题描述**:  
  1. 后台添加患者时，输入已存在的手机号（如 13800138002），系统未拦截，导致数据库中存在重复手机号的记录。  
  2. 小程序端新用户注册时，同一个手机号可以注册多个账号。  
  3. 小程序端修改个人信息时，可以将手机号修改为其他用户已使用的号码。  
* **修改方案**:  
  1. **数据库层**：在 user 或 patient 表的 phone 字段上添加 UNIQUE 唯一索引，从底层防止重复数据插入。  
  2. **API 逻辑层**：  
     * 在**注册接口** (/api/register) 和**新增患者接口**中，先执行 select count(\*) from users where phone \= ?。若大于 0，则直接返回错误码（如 409 Conflict）和提示信息“该手机号已注册”。  
     * 在**更新信息接口** (/api/user/update) 中，如果用户修改了手机号，需检查新号码是否已被 *其他* 用户（ID不等于当前用户ID）占用。

### **2\. 医生状态管理死锁**

* **Bug 编号**: BUG-DOC-001  
* **涉及模块**: 医生管理（后台）  
* **问题描述**: 当管理员点击“停用”按钮将医生账号停用后，列表中的操作栏未显示“启用”按钮，导致该医生账号无法恢复使用，流程被阻断。  
* **修改方案**:  
  1. **前端逻辑**：修改医生列表组件的操作列渲染逻辑。  
     * **当前逻辑**：可能仅判断了 if (status \=== 'active') show 'Disable'。  
     * **修正逻辑**：  
       // 示例逻辑  
       if (status \=== 'active') {  
           return \<Button onClick={handleDisable}\>停用\</Button\>;  
       } else if (status \=== 'disabled') {  
           return \<Button onClick={handleEnable}\>启用\</Button\>; // 补全此逻辑  
       }

  2. **后端接口**：确保后端存在 /api/doctor/enable 接口或通用的 /api/doctor/status/update 接口能够处理状态从 0 (停用) 到 1 (启用) 的变更。

## **🟡 中优先级 (影响主要功能)**

此类缺陷涉及业务逻辑错误或重要的输入校验缺失。

### **3\. 患者认证状态流转失败**

* **Bug 编号**: BUG-PATM-002  
* **涉及模块**: 患者管理（后台）  
* **问题描述**: 模拟对 ID 为 3 的患者进行认证操作时，点击确认后，列表中的“身份状态”字段仍显示为“待认证”，未能更新为“已认证”。  
* **修改方案**:  
  1. **排查后端**：检查审核接口 (approvePatient) 的 SQL 更新语句是否执行并提交事务。确认是否遗漏了更新 auth\_status 字段。  
  2. **排查前端**：检查操作成功后的回调函数。确认在收到后端 200 OK 响应后，是否重新请求了列表数据（Refresh List）或在本地更新了该条数据的状态显示。

### **4\. 注册与修改信息的边界值校验缺失**

* **Bug 编号**: BUG-PAT-05, BUG-PAT-01  
* **涉及模块**: 小程序端（注册/修改信息）  
* **问题描述**:  
  1. 用户注册时，未对用户名进行边界值校验（如长度限制、特殊字符限制）。  
  2. 修改信息时允许非法的手机号格式。  
* **修改方案**:  
  1. **前端增强**：  
     * **用户名**：限制长度为 4-20 字符，正则禁止特殊符号。  
     * **手机号**：使用正则 /^1\[3-9\]\\d{9}$/ 校验格式。  
  2. **后端兜底**：在 Controller 层使用 @Valid 或手动校验入参长度和格式，不符合要求直接拒绝请求。

## **🟢 低优先级 (UI/数据统计/体验问题)**

此类缺陷影响数据展示准确性或界面交互体验，但不阻碍核心流程。

### **5\. 首页数据统计不准确**

* **Bug 编号**: BUG-ADM-001 \~ BUG-ADM-005  
* **涉及模块**: 数据概览（后台首页）  
* **问题描述**: 首页卡片显示的“科室总数”、“医生数”、“今日预约数”、“注册患者数”及“本月收入”与数据库实际数据不一致。  
* **修改方案**:  
  1. **修正 SQL 查询**：逐一检查统计接口的 SQL 语句。  
     * **医生数**：确保过滤了 is\_deleted \= 0 和 status \= 'active'（根据需求决定是否包含停用医生）。  
     * **今日预约**：确保时间过滤条件是 WHERE date(appointment\_time) \= CURDATE()，注意时区问题。  
     * **本月收入**：确保仅统计 status \= 'paid/completed' 的订单，且时间范围正确。  
  2. **缓存策略**：如果使用了 Redis 缓存统计数据，需检查缓存刷新机制（定时刷新或事件触发刷新），避免显示过时数据。

### **6\. 预约趋势图加载失败**

* **Bug 编号**: BUG-ADM-006, BUG-ADM-007  
* **涉及模块**: 数据概览  
* **问题描述**: 预约趋势图区域空白或加载不出，时间筛选功能（本周/本月）点击无效。  
* **修改方案**:  
  1. **组件初始化**：确保 ECharts 实例是在 DOM 元素渲染完成后（Vue 的 mounted 或 React 的 useEffect）才进行初始化的。  
  2. **数据绑定**：检查日期筛选组件的 onChange 事件，确保它正确触发了 API 请求，并将返回的新数据通过 chart.setOption() 更新到图表中。

### **7\. 表单与输入框校验优化**

* **Bug 编号**: BUG-DEPT-001 (科室描述), BUG-DOC-002 (医生姓名), BUG-PAT-04 (真实姓名)  
* **涉及模块**: 科室管理、医生管理、小程序认证  
* **问题描述**:  
  * 科室描述无字数限制，可能导致数据库字段溢出或页面排版崩溃。  
  * 医生姓名允许 1 个字符或超长字符。  
  * 身份认证的“真实姓名”允许输入数字。  
* **修改方案**:  
  * **科室描述**：前端限制 maxlength="200"，并显示字数统计 0/200。  
  * **医生姓名**：限制长度 min=2, max=15。  
  * **真实姓名**：增加正则校验，仅允许汉字或英文字母（如 /^\[\\u4e00-\\u9fa5a-zA-Z\]+$/）。

### **8\. UI 功能缺失与跳转错误**

* **Bug 编号**: BUG-DOCW-001 (无修改密码), BUG-ADM-008 (通知无法跳转), BUG-PAT-03 (无门诊信息)  
* **涉及模块**: 医生工作台、后台通知、小程序查询  
* **问题描述**:  
  * 医生个人中心缺少修改密码按钮。  
  * 后台首页通知栏的“跳转管理”点击无反应。  
  * 小程序按科室查询时，结果列表中仅显示科室名，未列出下属门诊。  
* **修改方案**:  
  * **添加按钮**：在医生个人信息页添加“修改密码”按钮，并在点击时弹出修改密码模态框。  
  * **修复路由**：检查“跳转管理”按钮的 href 或 router-link，确保指向正确的路由路径（如 /system/notice）。  
  * **优化列表渲染**：修改小程序科室列表的 UI 模板，遍历展示该科室下的门诊数据（需确保 API 返回了关联的门诊列表）。