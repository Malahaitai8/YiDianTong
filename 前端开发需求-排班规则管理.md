# 📋 前端开发需求文档 - 排班规则管理模块

## 📑 目录
- [1. 功能概述](#1-功能概述)
- [2. 页面设计要求](#2-页面设计要求)
- [3. API 接口文档](#3-api-接口文档)
- [4. 数据字典](#4-数据字典)
- [5. 开发建议](#5-开发建议)
- [6. 测试要点](#6-测试要点)

---

## 1. 功能概述

### 1.1 业务背景
排班规则管理是一个**自动化批量排班工具**，允许管理员创建排班规则模板，系统根据规则自动生成大量排班记录，避免手动逐个创建的繁琐操作。

### 1.2 核心功能
- ✅ **规则列表展示**：查看所有排班规则
- ✅ **规则创建**：创建新的排班规则
- ✅ **规则详情**：查看规则完整信息（含关联的医生、科室、门诊）
- ✅ **规则编辑**：修改现有规则
- ✅ **规则删除**：删除规则（不影响已生成的排班）
- ✅ **规则启用/禁用**：控制规则状态
- ✅ **应用规则**：根据规则批量生成排班
- ✅ **冲突检测**：检查规则是否与现有规则冲突

### 1.3 用户角色
- **仅管理员**可以访问此模块（需要 `ROLE_ADMIN` 权限）

---

## 2. 页面设计要求

### 2.1 页面结构

```
管理后台
├── 排班管理
│   ├── 排班规则列表（主页面）
│   ├── 创建排班规则（弹窗/新页面）
│   ├── 规则详情（弹窗/侧边栏）
│   └── 应用规则（弹窗）
```

---

### 2.2 【页面1】排班规则列表

#### 页面布局
```
┌─────────────────────────────────────────────────────────┐
│  排班规则管理                           [+ 创建规则] 按钮 │
├─────────────────────────────────────────────────────────┤
│  筛选条件：                                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                  │
│  │医生  │ │科室  │ │状态  │ │规则类型│  [搜索] [重置] │
│  └──────┘ └──────┘ └──────┘ └──────┘                  │
├─────────────────────────────────────────────────────────┤
│  规则列表（表格）                                        │
│  ┌────┬────────┬──────┬────────┬────┬────┬──────┐     │
│  │序号│规则名称│规则类型│医生/科室│状态│优先级│操作  │     │
│  ├────┼────────┼──────┼────────┼────┼────┼──────┤     │
│  │ 1  │心内科  │固定周│王医生  │启用│ 10 │详情  │     │
│  │    │专家号  │排班  │(心内科)│    │    │编辑  │     │
│  │    │        │      │        │    │    │应用  │     │
│  │    │        │      │        │    │    │禁用  │     │
│  │    │        │      │        │    │    │删除  │     │
│  └────┴────────┴──────┴────────┴────┴────┴──────┘     │
│                                    [上一页] [下一页]    │
└─────────────────────────────────────────────────────────┘
```

#### 表格字段
| 字段 | 说明 | 数据来源 | 备注 |
|------|------|----------|------|
| 规则名称 | `ruleName` | 接口返回 | 可点击查看详情 |
| 规则类型 | `ruleTypeName` | 接口返回 | 中文显示：固定排班/轮班制/自定义 |
| 医生/科室 | `doctorName` 或 `departmentName` | 接口返回 | 如果有医生显示医生，否则显示科室 |
| 时间配置 | `weekDaysDisplay` + `timeSlotsDisplay` | 接口返回 | 如："周一三五 上午下午" |
| 状态 | `statusName` | 接口返回 | 启用/禁用/已过期（带颜色标签） |
| 优先级 | `priority` | 接口返回 | 数字，越大优先级越高 |
| 生效期间 | `startDate` - `endDate` | 接口返回 | 格式：2025-11-01 ~ 2025-12-31 |

#### 操作按钮
| 操作 | 说明 | 条件 |
|------|------|------|
| 详情 | 查看规则完整信息 | 所有规则 |
| 编辑 | 修改规则 | 所有规则 |
| 应用 | 根据规则生成排班 | 仅启用状态 |
| 启用/禁用 | 切换规则状态 | 所有规则 |
| 删除 | 删除规则 | 所有规则（需二次确认） |

---

### 2.3 【页面2】创建/编辑排班规则

#### 表单布局（分步骤或分区域）

```
┌─────────────────────────────────────────────────────────┐
│  创建排班规则                               [关闭] ×     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  【基本信息】                                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 规则名称：[________________________] (必填)      │   │
│  │ 规则类型：[固定周排班 ▼] (必填)                 │   │
│  │           - 固定周排班：每周固定某几天           │   │
│  │           - 轮班制：医生按顺序轮流               │   │
│  │           - 自定义：灵活定义                     │   │
│  │ 规则描述：[________________________]             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  【关联信息】                                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 医生：[请选择医生 ▼] (可选，不选表示科室通用)   │   │
│  │ 科室：[请选择科室 ▼] (可选)                     │   │
│  │ 门诊：[请选择门诊 ▼] (可选)                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  【时间配置】                                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 星期：☐周一 ☐周二 ☐周三 ☐周四 ☐周五 ☐周六 ☐周日  │   │
│  │ 时段：☐上午 ☐下午 ☐晚间 (必填)                  │   │
│  │ 生效日期：[2025-11-01] 至 [2025-12-31]          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  【号源配置】                                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 号别类型：⚫普通号 ⚪专家号 ⚪特需号 (必填)       │   │
│  │ 总号源数：[___20___] (1-100，必填)              │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  【高级规则】(可折叠)                                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 每天最多排班次数：[___] (可选)                   │   │
│  │ 最多连续出诊天数：[___] (可选)                   │   │
│  │ ☐ 跳过周末                                       │   │
│  │ ☐ 跳过节假日                                     │   │
│  │ 优先级：[___0___] (数字越大优先级越高)          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│                          [取消] [保存并启用]             │
└─────────────────────────────────────────────────────────┘
```

#### 表单字段详细说明

| 字段名 | 字段类型 | 是否必填 | 校验规则 | 默认值 |
|--------|----------|----------|----------|--------|
| `ruleName` | 文本 | ✅ | 不超过100字符 | - |
| `ruleType` | 下拉单选 | ✅ | FIXED_WEEKLY/ROTATION/CUSTOM | FIXED_WEEKLY |
| `doctorId` | 下拉单选 | ❌ | 需要先选择科室，再加载该科室下的医生 | null |
| `departmentId` | 下拉单选 | ❌ | - | null |
| `clinicId` | 下拉单选 | ❌ | - | null |
| `weekDays` | 多选框 | ❌ | 至少选一个（如果选了） | [] |
| `timeSlots` | 多选框 | ✅ | 至少选一个 | [] |
| `startDate` | 日期选择器 | ✅ | 不能早于今天 | 今天 |
| `endDate` | 日期选择器 | ❌ | 必须晚于开始日期 | null |
| `slotType` | 单选按钮 | ✅ | normal/expert/vip | normal |
| `totalSlots` | 数字输入 | ✅ | 1-100的整数 | 20 |
| `maxDailySchedules` | 数字输入 | ❌ | 1-10的整数 | null |
| `maxContinuousDays` | 数字输入 | ❌ | 1-30的整数 | null |
| `skipWeekends` | 复选框 | ❌ | - | false |
| `skipHolidays` | 复选框 | ❌ | - | false |
| `priority` | 数字输入 | ❌ | 0-100的整数 | 0 |
| `description` | 多行文本 | ❌ | 不超过500字符 | "" |

#### 提交前校验
- 规则名称不能为空
- 至少选择一个时段
- 总号源数必须在 1-100 之间
- 结束日期必须晚于开始日期（如果有）
- 如果选择了星期，至少选一天

---

### 2.4 【页面3】规则详情页/弹窗

#### 布局
```
┌─────────────────────────────────────────────────────────┐
│  规则详情                                   [关闭] ×     │
├─────────────────────────────────────────────────────────┤
│  基本信息                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 规则名称：心内科专家号排班                       │   │
│  │ 规则类型：固定排班                               │   │
│  │ 规则状态：🟢 启用中                              │   │
│  │ 优先级：10                                       │   │
│  │ 规则描述：每周一、三、五上午和下午出诊           │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  关联信息                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 医生：王医生 (主任医师)                          │   │
│  │ 科室：心内科                                     │   │
│  │ 门诊：专家门诊                                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  时间配置                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 星期：周一、周三、周五                           │   │
│  │ 时段：上午、下午                                 │   │
│  │ 生效期间：2025-11-01 至 2025-12-31              │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  号源配置                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 号别：专家号                                     │   │
│  │ 总号源数：20                                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  高级规则                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 每天最多排班次数：2                              │   │
│  │ 最多连续出诊天数：5                              │   │
│  │ 跳过周末：是                                     │   │
│  │ 跳过节假日：是                                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  审计信息                                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 创建人：admin                                    │   │
│  │ 创建时间：2025-10-31 10:00:00                   │   │
│  │ 更新时间：2025-10-31 10:00:00                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│              [编辑] [应用规则] [启用/禁用] [删除]        │
└─────────────────────────────────────────────────────────┘
```

---

### 2.5 【页面4】应用规则弹窗

#### 布局
```
┌─────────────────────────────────────────────────────────┐
│  应用规则生成排班                           [关闭] ×     │
├─────────────────────────────────────────────────────────┤
│  即将应用规则：心内科专家号排班                          │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 应用日期范围：                                   │   │
│  │ [2025-11-01] 至 [2025-11-30] (必填)             │   │
│  │                                                  │   │
│  │ ☐ 覆盖已有排班（谨慎使用）                       │   │
│  │                                                  │   │
│  │ 排除日期（可选）：                               │   │
│  │ [+ 添加日期]                                     │   │
│  │ × 2025-11-05 (已添加)                           │   │
│  │                                                  │   │
│  │ 预计生成排班数：约 24 条                         │   │
│  │ (根据规则和日期范围自动计算)                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ⚠️ 提示：                                               │
│  - 生成的排班将立即生效                                  │
│  - 如果选择覆盖，将删除该时间范围内的现有排班            │
│  - 建议先检测冲突：[检测冲突]                            │
│                                                          │
│                              [取消] [确认生成]           │
└─────────────────────────────────────────────────────────┘
```

#### 应用成功后提示
```
┌─────────────────────────────────────────┐
│  ✅ 排班生成成功                         │
│                                          │
│  成功生成：20 条                         │
│  跳过：3 条（已存在）                    │
│  失败：1 条（查看详情）                  │
│                                          │
│          [查看生成的排班] [关闭]         │
└─────────────────────────────────────────┘
```

---

## 3. API 接口文档

### 3.1 接口基础信息

**Base URL**: `http://localhost:9090/api/admin`

**认证方式**: Bearer Token (JWT)
- 请求头：`Authorization: Bearer <token>`

**权限要求**: 所有接口需要 `ROLE_ADMIN` 权限

---

### 3.2 接口列表

#### 3.2.1 查询所有排班规则

**接口**: `GET /schedule-rules-new`

**说明**: 获取所有排班规则列表，包含关联信息和中文显示

**请求参数**: 无

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "ruleName": "心内科专家号排班",
      "ruleType": "FIXED",
      "ruleTypeName": "固定排班",
      "doctorId": 1,
      "doctorName": "王医生",
      "doctorTitle": "主任医师",
      "departmentId": 1,
      "departmentName": "心内科",
      "clinicId": 1,
      "clinicName": "专家门诊",
      "weekDays": "1,3,5",
      "weekDaysList": [1, 3, 5],
      "weekDaysDisplay": "周一、周三、周五",
      "timeSlots": "MORNING,AFTERNOON",
      "timeSlotsList": ["MORNING", "AFTERNOON"],
      "timeSlotsDisplay": "上午、下午",
      "slotType": "EXPERT",
      "slotTypeName": "专家号",
      "totalSlots": 20,
      "maxDailySchedules": 2,
      "maxContinuousDays": 5,
      "skipWeekends": true,
      "skipHolidays": true,
      "startDate": "2025-11-01",
      "endDate": "2025-12-31",
      "status": "ACTIVE",
      "statusName": "启用",
      "priority": 10,
      "description": "每周一、三、五上午和下午出诊",
      "createdBy": "admin",
      "createdAt": "2025-10-31 10:00:00",
      "updatedAt": "2025-10-31 10:00:00"
    }
  ]
}
```

---

#### 3.2.2 查询规则详情

**接口**: `GET /schedule-rules-new/{ruleId}`

**说明**: 查询单个规则的完整详情

**路径参数**:
- `ruleId`: 规则ID（数字）

**响应示例**: 同 3.2.1 单个对象

---

#### 3.2.3 创建排班规则

**接口**: `POST /schedule-rules-new`

**说明**: 创建新的排班规则

**请求体**:
```json
{
  "ruleName": "心内科专家号排班",
  "ruleType": "FIXED_WEEKLY",
  "doctorId": 1,
  "departmentId": 1,
  "clinicId": 1,
  "weekDays": [1, 3, 5],
  "timeSlots": ["MORNING", "AFTERNOON"],
  "slotType": "EXPERT",
  "totalSlots": 20,
  "maxDailySchedules": 2,
  "maxContinuousDays": 5,
  "skipWeekends": true,
  "skipHolidays": true,
  "startDate": "2025-11-01",
  "endDate": "2025-12-31",
  "priority": 10,
  "description": "每周一、三、五上午和下午出诊"
}
```

**字段说明**:
- `ruleName` (必填): 规则名称
- `ruleType` (必填): 规则类型
  - `FIXED_WEEKLY` - 固定周排班
  - `ROTATION` - 轮班制
  - `CUSTOM` - 自定义
- `doctorId` (可选): 医生ID
- `departmentId` (可选): 科室ID
- `clinicId` (可选): 门诊ID
- `weekDays` (可选): 星期数组，1=周一, 7=周日
- `timeSlots` (必填): 时段数组 `["MORNING", "AFTERNOON", "EVENING"]`
- `slotType` (必填): 号别
  - `NORMAL` - 普通号
  - `EXPERT` - 专家号
  - `VIP` - 特需号
- `totalSlots` (必填): 总号源数 (1-100)
- `maxDailySchedules` (可选): 每天最多排班次数
- `maxContinuousDays` (可选): 最多连续天数
- `skipWeekends` (可选): 是否跳过周末
- `skipHolidays` (可选): 是否跳过节假日
- `startDate` (必填): 开始日期 (yyyy-MM-dd)
- `endDate` (可选): 结束日期
- `priority` (可选): 优先级
- `description` (可选): 描述

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": {
    "id": 1,
    "ruleName": "心内科专家号排班",
    ...
  }
}
```

---

#### 3.2.4 更新排班规则

**接口**: `PUT /schedule-rules-new/{ruleId}`

**说明**: 更新现有规则（部分更新）

**路径参数**:
- `ruleId`: 规则ID

**请求体**: 同创建接口，但所有字段都是可选的，只传需要修改的字段

**响应示例**: 返回更新后的完整规则

---

#### 3.2.5 删除排班规则

**接口**: `DELETE /schedule-rules-new/{ruleId}`

**说明**: 删除规则（不影响已生成的排班）

**路径参数**:
- `ruleId`: 规则ID

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": "规则删除成功"
}
```

---

#### 3.2.6 启用规则

**接口**: `POST /schedule-rules-new/{ruleId}/enable`

**说明**: 将规则状态改为 ACTIVE

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": "规则已启用"
}
```

---

#### 3.2.7 禁用规则

**接口**: `POST /schedule-rules-new/{ruleId}/disable`

**说明**: 将规则状态改为 INACTIVE

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": "规则已禁用"
}
```

---

#### 3.2.8 应用规则生成排班 ⭐

**接口**: `POST /schedule-rules-new/{ruleId}/apply`

**说明**: 根据规则批量生成排班记录

**路径参数**:
- `ruleId`: 规则ID

**请求体**:
```json
{
  "applyStartDate": "2025-11-01",
  "applyEndDate": "2025-11-30",
  "overwriteExisting": false,
  "excludeDates": ["2025-11-05", "2025-11-15"]
}
```

**字段说明**:
- `applyStartDate` (可选): 应用开始日期，默认使用规则的 startDate
- `applyEndDate` (可选): 应用结束日期，默认使用规则的 endDate 或开始日期+30天
- `overwriteExisting` (可选): 是否覆盖已有排班，默认 false
- `excludeDates` (可选): 排除的日期数组

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": {
    "successCount": 20,
    "skipCount": 3,
    "errorCount": 1,
    "errors": [
      "2025-11-10 上午: 该时间段已有排班"
    ]
  }
}
```

---

#### 3.2.9 检测规则冲突

**接口**: `GET /schedule-rules-new/{ruleId}/conflicts`

**说明**: 检查规则是否与现有排班或其他规则冲突

**路径参数**:
- `ruleId`: 规则ID

**查询参数**:
- `startDate` (可选): 检测开始日期
- `endDate` (可选): 检测结束日期

**响应示例**:
```json
{
  "code": "200",
  "msg": "成功",
  "data": {
    "hasConflict": true,
    "conflicts": [
      {
        "date": "2025-11-05",
        "timeSlot": "MORNING",
        "conflictType": "EXISTING_SCHEDULE",
        "message": "该时段已有排班"
      }
    ]
  }
}
```

---

#### 3.2.10 查询医生的规则

**接口**: `GET /schedule-rules-new/doctor/{doctorId}`

**说明**: 查询指定医生的所有规则

**路径参数**:
- `doctorId`: 医生ID

**响应示例**: 返回规则数组

---

#### 3.2.11 按状态查询规则

**接口**: `GET /schedule-rules-new/status/{status}`

**说明**: 查询指定状态的规则

**路径参数**:
- `status`: 状态 (ACTIVE/INACTIVE)

**响应示例**: 返回规则数组

---

### 3.3 辅助接口（用于下拉选择）

#### 获取医生列表
```
GET /api/admin/doctors
GET /api/admin/doctors/department/{departmentId}  // 按科室筛选
```

#### 获取科室列表
```
GET /api/admin/departments
```

#### 获取门诊列表
```
GET /api/admin/clinics
```

---

## 4. 数据字典

### 4.1 规则类型 (ruleType)

| 英文值 | 中文名称 | 说明 |
|--------|----------|------|
| `FIXED_WEEKLY` | 固定周排班 | 每周固定某几天出诊 |
| `ROTATION` | 轮班制 | 医生按顺序轮流排班 |
| `CUSTOM` | 自定义规则 | 灵活定义排班逻辑 |

### 4.2 时段 (timeSlot)

| 英文值 | 中文名称 | 时间范围 |
|--------|----------|----------|
| `MORNING` | 上午 | 08:00-12:00 |
| `AFTERNOON` | 下午 | 14:00-18:00 |
| `EVENING` | 晚间 | 18:00-22:00 |

### 4.3 号别 (slotType)

| 英文值 | 中文名称 | 挂号费 | 说明 |
|--------|----------|--------|------|
| `NORMAL` | 普通号 | 10元 | 普通门诊 |
| `EXPERT` | 专家号 | 50元 | 主任医师、副主任医师 |
| `VIP` | 特需号 | 200元 | VIP门诊、特需服务 |

### 4.4 规则状态 (status)

| 英文值 | 中文名称 | 颜色标签 | 说明 |
|--------|----------|----------|------|
| `ACTIVE` | 启用 | 🟢 绿色 | 可以应用生成排班 |
| `INACTIVE` | 禁用 | 🔴 红色 | 暂时停用，不可应用 |
| `EXPIRED` | 已过期 | ⚫ 灰色 | 结束日期已过，系统自动标记 |

### 4.5 星期 (weekDay)

| 数字 | 中文 |
|------|------|
| 1 | 周一 |
| 2 | 周二 |
| 3 | 周三 |
| 4 | 周四 |
| 5 | 周五 |
| 6 | 周六 |
| 7 | 周日 |

---

## 5. 开发建议

### 5.1 技术栈建议
- **前端框架**: Vue 3 / React 18
- **UI 组件库**: Element Plus / Ant Design
- **状态管理**: Pinia / Zustand
- **HTTP 客户端**: Axios
- **日期处理**: Day.js
- **表单校验**: 使用组件库自带的表单验证

### 5.2 封装建议

#### API 封装示例 (axios)
```javascript
// api/scheduleRule.js
import request from '@/utils/request'

export const scheduleRuleApi = {
  // 查询所有规则
  getAllRules() {
    return request.get('/api/admin/schedule-rules-new')
  },
  
  // 查询规则详情
  getRuleById(ruleId) {
    return request.get(`/api/admin/schedule-rules-new/${ruleId}`)
  },
  
  // 创建规则
  createRule(data) {
    return request.post('/api/admin/schedule-rules-new', data)
  },
  
  // 更新规则
  updateRule(ruleId, data) {
    return request.put(`/api/admin/schedule-rules-new/${ruleId}`, data)
  },
  
  // 删除规则
  deleteRule(ruleId) {
    return request.delete(`/api/admin/schedule-rules-new/${ruleId}`)
  },
  
  // 启用规则
  enableRule(ruleId) {
    return request.post(`/api/admin/schedule-rules-new/${ruleId}/enable`)
  },
  
  // 禁用规则
  disableRule(ruleId) {
    return request.post(`/api/admin/schedule-rules-new/${ruleId}/disable`)
  },
  
  // 应用规则
  applyRule(ruleId, data) {
    return request.post(`/api/admin/schedule-rules-new/${ruleId}/apply`, data)
  },
  
  // 检测冲突
  checkConflicts(ruleId, params) {
    return request.get(`/api/admin/schedule-rules-new/${ruleId}/conflicts`, { params })
  }
}
```

#### 枚举值处理
```javascript
// constants/scheduleRule.js
export const RULE_TYPE = {
  FIXED_WEEKLY: '固定周排班',
  ROTATION: '轮班制',
  CUSTOM: '自定义规则'
}

export const TIME_SLOT = {
  MORNING: '上午',
  AFTERNOON: '下午',
  EVENING: '晚间'
}

export const SLOT_TYPE = {
  NORMAL: '普通号',
  EXPERT: '专家号',
  VIP: '特需号'
}

export const STATUS = {
  ACTIVE: { text: '启用', color: 'success' },
  INACTIVE: { text: '禁用', color: 'danger' },
  EXPIRED: { text: '已过期', color: 'info' }
}

export const WEEK_DAYS = [
  { value: 1, label: '周一' },
  { value: 2, label: '周二' },
  { value: 3, label: '周三' },
  { value: 4, label: '周四' },
  { value: 5, label: '周五' },
  { value: 6, label: '周六' },
  { value: 7, label: '周日' }
]
```

### 5.3 数据转换

#### 提交时：数组 → 字符串
```javascript
// 前端表单数据
const formData = {
  weekDays: [1, 3, 5],
  timeSlots: ['MORNING', 'AFTERNOON']
}

// 提交给后端时无需转换，直接传数组即可
// 后端会自动处理
```

#### 显示时：使用接口返回的中文字段
```javascript
// 接口返回的数据已包含中文显示
const rule = {
  weekDays: "1,3,5",
  weekDaysList: [1, 3, 5],
  weekDaysDisplay: "周一、周三、周五",  // 直接使用这个
  
  timeSlots: "MORNING,AFTERNOON",
  timeSlotsList: ["MORNING", "AFTERNOON"],
  timeSlotsDisplay: "上午、下午",  // 直接使用这个
  
  status: "ACTIVE",
  statusName: "启用"  // 直接使用这个
}
```

### 5.4 表单校验规则

```javascript
// 表单校验规则示例
const rules = {
  ruleName: [
    { required: true, message: '请输入规则名称', trigger: 'blur' },
    { max: 100, message: '规则名称不超过100字符', trigger: 'blur' }
  ],
  ruleType: [
    { required: true, message: '请选择规则类型', trigger: 'change' }
  ],
  timeSlots: [
    { required: true, message: '请至少选择一个时段', trigger: 'change' },
    { type: 'array', min: 1, message: '请至少选择一个时段', trigger: 'change' }
  ],
  slotType: [
    { required: true, message: '请选择号别类型', trigger: 'change' }
  ],
  totalSlots: [
    { required: true, message: '请输入总号源数', trigger: 'blur' },
    { type: 'number', min: 1, max: 100, message: '号源数必须在1-100之间', trigger: 'blur' }
  ],
  startDate: [
    { required: true, message: '请选择开始日期', trigger: 'change' }
  ]
}
```

### 5.5 错误处理

```javascript
// 统一错误处理
async function handleCreateRule(formData) {
  try {
    const res = await scheduleRuleApi.createRule(formData)
    if (res.code === '200') {
      ElMessage.success('规则创建成功')
      // 刷新列表或跳转
    } else {
      ElMessage.error(res.msg || '创建失败')
    }
  } catch (error) {
    console.error('创建规则失败:', error)
    ElMessage.error('网络错误，请稍后重试')
  }
}
```

---

## 6. 测试要点

### 6.1 功能测试
- [ ] 规则列表正常展示
- [ ] 筛选功能正常工作
- [ ] 创建规则成功
- [ ] 编辑规则成功
- [ ] 删除规则成功（需二次确认）
- [ ] 启用/禁用状态切换正常
- [ ] 应用规则生成排班成功
- [ ] 冲突检测功能正常
- [ ] 详情页展示完整信息

### 6.2 表单校验测试
- [ ] 必填字段不填提示错误
- [ ] 号源数超出范围提示错误
- [ ] 结束日期早于开始日期提示错误
- [ ] 规则名称超长提示错误
- [ ] 未选时段提示错误

### 6.3 边界情况测试
- [ ] 没有规则时的空状态展示
- [ ] 规则数量很多时的分页
- [ ] 应用规则但没有生成排班的情况
- [ ] 规则已过期的展示
- [ ] 删除规则失败的提示
- [ ] 应用规则时选择覆盖的二次确认

### 6.4 权限测试
- [ ] 非管理员访问返回 403
- [ ] Token 过期跳转登录
- [ ] 未登录访问拦截

### 6.5 用户体验测试
- [ ] 加载状态提示（loading）
- [ ] 操作成功提示（toast/message）
- [ ] 操作失败提示（toast/message）
- [ ] 按钮禁用状态（防止重复提交）
- [ ] 表单重置功能
- [ ] 响应式布局（移动端适配）

---

## 7. 附录

### 7.1 完整的请求示例

#### 创建规则完整示例
```javascript
// 1. 准备表单数据
const formData = {
  ruleName: "心内科专家号排班",
  ruleType: "FIXED_WEEKLY",
  doctorId: 1,
  departmentId: 1,
  clinicId: 1,
  weekDays: [1, 3, 5],
  timeSlots: ["MORNING", "AFTERNOON"],
  slotType: "EXPERT",
  totalSlots: 20,
  maxDailySchedules: 2,
  maxContinuousDays: 5,
  skipWeekends: true,
  skipHolidays: true,
  startDate: "2025-11-01",
  endDate: "2025-12-31",
  priority: 10,
  description: "每周一、三、五上午和下午出诊"
}

// 2. 调用API
const response = await scheduleRuleApi.createRule(formData)

// 3. 处理响应
if (response.code === '200') {
  console.log('创建成功，规则ID:', response.data.id)
}
```

#### 应用规则完整示例
```javascript
// 1. 准备应用参数
const applyData = {
  applyStartDate: "2025-11-01",
  applyEndDate: "2025-11-30",
  overwriteExisting: false,
  excludeDates: ["2025-11-05"]
}

// 2. 先检测冲突（可选）
const conflictRes = await scheduleRuleApi.checkConflicts(ruleId, {
  startDate: applyData.applyStartDate,
  endDate: applyData.applyEndDate
})

if (conflictRes.data.hasConflict) {
  // 显示冲突信息，询问是否继续
  console.log('检测到冲突:', conflictRes.data.conflicts)
}

// 3. 应用规则
const applyRes = await scheduleRuleApi.applyRule(ruleId, applyData)

if (applyRes.code === '200') {
  console.log(`成功生成 ${applyRes.data.successCount} 条排班`)
  console.log(`跳过 ${applyRes.data.skipCount} 条`)
  console.log(`失败 ${applyRes.data.errorCount} 条`)
}
```

### 7.2 联系后端开发
如果有任何接口问题或字段疑问，请联系后端开发人员。

**已实现的接口文档**: `API文档.md` 第 13 章节

---

## 8. 快速开始清单

### 前端开发人员需要做的事：

#### 第一步：熟悉业务
- [ ] 阅读本文档，理解排班规则的概念
- [ ] 查看 API 文档第 13 章
- [ ] 了解数据字典中的枚举值

#### 第二步：准备工作
- [ ] 搭建开发环境
- [ ] 配置 API 基础URL
- [ ] 封装 API 请求方法
- [ ] 准备枚举常量文件

#### 第三步：页面开发
- [ ] 开发规则列表页
- [ ] 开发创建/编辑规则表单
- [ ] 开发规则详情页
- [ ] 开发应用规则弹窗
- [ ] 集成到管理后台路由

#### 第四步：测试联调
- [ ] 本地调试所有功能
- [ ] 与后端联调接口
- [ ] 修复 Bug
- [ ] 提交测试

---

## 9. 常见问题 FAQ

### Q1: weekDays 传数组还是字符串？
**A**: 创建和更新时传**数组** `[1, 3, 5]`，后端自动转换。接口返回时会包含三种格式，直接使用 `weekDaysDisplay` 显示中文。

### Q2: 时段的英文值是什么？
**A**: `MORNING`（上午）、`AFTERNOON`（下午）、`EVENING`（晚间），注意大写。

### Q3: 规则状态如何展示？
**A**: 接口返回的 `statusName` 字段已是中文（"启用"/"禁用"/"已过期"），直接使用。
- 启用：绿色标签
- 禁用：红色标签  
- 已过期：灰色标签

### Q4: 应用规则时如何知道会生成多少排班？
**A**: 可以在前端根据日期范围、星期配置、时段数量简单估算，或调用冲突检测接口获取准确数量。

### Q5: 删除规则会影响已生成的排班吗？
**A**: **不会**。删除规则只是删除模板，已生成的排班记录不受影响。

### Q6: 医生下拉列表如何获取？
**A**: 调用 `GET /api/admin/doctors/department/{departmentId}` 根据选择的科室动态加载医生列表。

---

## 10. 总结

本文档提供了**排班规则管理模块**的完整前端开发需求，包括：

✅ 业务逻辑说明  
✅ 页面设计原型  
✅ API 接口文档  
✅ 数据字典  
✅ 代码封装建议  
✅ 测试要点  

**关键点提醒**：
1. 接口返回的数据已包含**中文显示字段**，直接使用即可
2. 表单提交时传**数组**格式（weekDays, timeSlots）
3. 所有接口需要 **ROLE_ADMIN** 权限
4. 应用规则前建议先**检测冲突**
5. 删除操作需要**二次确认**

如有疑问，请随时与后端开发人员沟通！🚀

