<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.AppointmentMigrationAuditMapper">
    <insert id="insert" parameterType="com.example.springboot.entity.AppointmentMigrationAudit" useGeneratedKeys="true" keyProperty="id">
        insert into appointment_migration_audit(appointment_id, from_schedule_id, to_schedule_id, operator_id, note, created_at)
        values(#{appointmentId}, #{fromScheduleId}, #{toScheduleId}, #{operatorId}, #{note}, now())
    </insert>
</mapper>


